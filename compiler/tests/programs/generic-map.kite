fun map1<A, B>(xs: List, f: (A) -> B): List {
  val out = list()
  for (x in xs) { out.push(f(x)) }
  return out
}

fun main(): Int {
  val xs = [1, 2, 3, 4]
  val ys = map1(xs, { n -> n * n })
  return ys.get(0) + ys.get(3)
}
