const val MAX = 100

const val SHIFT = 1 << 4

const val NEG = 0 - 5

const val COMBO = (10 + 5) * 3

fun useConst(): Int { return MAX + COMBO }

fun main(): Int {
  return useConst() + SHIFT + NEG
}
