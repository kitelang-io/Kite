const val HLEN = "hello".len()

fun main(): Int {
  val a = comptime("hi".len() + "world".len())
  val b = comptime("ABC".charAt(1))
  val c = comptime([10, 20, 30].len())
  val d = comptime([10, 20, 30].get(2))
  return a + b + c + d + HLEN
}
