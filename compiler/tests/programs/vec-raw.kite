import kite::alloc::collections::vec

fun main(): Int {
  val v = vecNew()
  var i = 0
  while (i < 20) { v.push(i * 2) i = i + 1 } // method call inside a module
  v.set(5, 100)
  return v.get(5) + v.get(19) + v.len() // 100 + 38 + 20 = 158
}
