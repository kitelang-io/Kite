class Res(val id: Int, val log: IntBuf) {
  deinit { self.log.set(0, self.log.get(0) + 1) }
}

fun main(): Int {
  val log = list()
  log.push(0)
  var r = Res(1, log)
  r = Res(2, log)
  r = Res(3, log)
  val mid = log.get(0)
  return mid * 10 + r.id
}
